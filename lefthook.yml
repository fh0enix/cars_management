code_quality_control:
 parallel: true
 commands:
    rubocop:
        run: bundle exec rubocop
    bundler_leak:
        run: bundle exec bundle-leak
    bundler_audit:
        run: bundle exec bundler-audit
